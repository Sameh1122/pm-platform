<!DOCTYPE html>
<html>
<head>
  <title><%= title || 'Message' %></title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <div class="container" style="max-width:680px">
    <div class="card">
      <h2 style="margin:0 0 8px 0"><%= title || 'Message' %></h2>
      <p><%= body || '' %></p>
      <% if (backHref) { %>
        <p><a class="btn" href="<%= backHref %>"><%= backText || 'Back' %></a></p>
      <% } %>
    </div>
  </div>
</body>
</html>
